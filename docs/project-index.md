# Индекс проекта DataConsole83

Состояние индекса: 14 сентября 2026 года.

## Назначение

**Консоль запросов для 1С с редактором на базе Monaco** — внешняя управляемая обработка `DtCons83Monaco`. Она хранит дерево алгоритмов, выполняет запросы и произвольный BSL-код на клиенте, сервере или в фоне, показывает результаты и временные таблицы, а Monaco обеспечивает подсветку, подсказки, навигацию, форматирование и отображение ошибок.

Версия в метаданных обработки: `4.0.0`. Исходные проекты, указанные в комментариях кода: `dtcons83.epf` и `bsl_console`.

## Карта исходников

| Путь | Ответственность |
|---|---|
| `src/epf/DtCons83Monaco/DtCons83Monaco.xml` | Корень метаданных внешней обработки, формы и макеты |
| `src/epf/DtCons83Monaco/DtCons83Monaco/Ext/ObjectModule.bsl` | Регистрация обработки, версия, получение форм |
| `src/epf/DtCons83Monaco/DtCons83Monaco/Forms/Основная/Ext/Form/Module.bsl` | Главный application service: редакторы, выполнение запроса/кода, фоновые задания, файлы алгоритмов, результаты |
| `Forms/ФормаКодаЗапроса/` | Вспомогательная форма ввода кода запроса |
| `Forms/ФормаВложеннойТаблицы/` | Просмотр и редактирование вложенных таблиц и хранилищ |
| `Templates/src/Ext/Template.bin` | ZIP-пакет web-редактора, который реально извлекается при запуске |
| `Templates/src2/Ext/Template.bin` | Второй вариант пакета; прямое использование BSL-кодом не найдено |
| `Templates/МакетЗапроса/` | Шаблон текста запроса |
| `Templates/ПользовательскиеФункции/` | Пользовательские функции редактора |
| `Templates/ПользовательскиеСниппеты/` | Пользовательские сниппеты BSL |
| `src/code/index.html` | Страница Monaco и дерево переменных |
| `src/code/editor.js` | Жизненный цикл Monaco, действия, модели, diff, маркеры и события |
| `src/code/bsl_language.js`, `bsl_helper.js` | Регистрация языка BSL и интеграционная логика редактора |
| `src/code/bslQuery.js`, `bslDCS.js` | Поддержка языка запросов и СКД |
| `src/code/bslGlobals.js`, `bslMetadata.js` | Большие сгенерированные/справочные данные подсказок |
| `src/code/tree/` | Дерево значений переменных |
| `src/code/vs/` | Vendored runtime Monaco; изменять только при осознанном обновлении зависимости |
| `tools/compileEpfErf.cmd`, `decompileEpfErf.cmd` | Сборка и разбор EPF через `vanessa-runner` |

## Ключевые потоки

### Запуск редактора

1. `ПриСозданииНаСервере` помещает макет `src` во временное хранилище.
2. Клиентская инициализация записывает пакет как `bsl_console.zip`, извлекает файлы и загружает `index.html` в поля HTML-документа.
3. `ИнициализацияРедактора` связывает форму с JavaScript API. `View()`, `QueryView()` и `CodeView()` возвращают `defaultView` соответствующего HTML-документа.
4. `ОбработатьСобытиеРедактора` принимает события Monaco и переносит состояние в форму 1С.

### Выполнение

- Команды `ВыполнитьЗапрос*` передают текст в серверный контур, формируют результаты и адреса временного хранилища.
- `ВыполнитьКод` маршрутизирует BSL между клиентом, сервером и фоновым выполнением.
- `ОбработатьРезультатВыполнения`, `ОбновитьФорму` и `ОтобразитьРезультатВременнойТаблицы` обновляют UI и диагностические данные.

## Инварианты и риски

- `src/code/` и `Template.bin` — два представления одного web-пакета. На момент индекса в обоих архивах по 57 файлов; рабочая копия отличается от `src` файлом `bsl_helper.js`, а от `src2` — `bsl_helper.js` и `index.html`. Перед коммитом нужно явно определить источник истины и проверить состав ZIP.
- Изменение имени JavaScript-функции, события или DOM-элемента требует проверки вызовов из BSL и HTML.
- Обработка выполняет произвольный код. Нельзя ослаблять безопасный режим, расширять контекст выполнения или выводить секретные значения без отдельного решения по безопасности.
- Жизненный цикл временных файлов и адресов хранилища должен завершаться очисткой и корректно работать при повторной инициализации.
- `src/code/test.html` ссылается на отсутствующий `test.js`; готового автоматического web-test runner нет.

## Навигация для агента

- Изменение BSL/XML: `.cursor/skills/change-bsl-epf/SKILL.md`.
- Изменение Monaco/HTML/CSS: `.cursor/skills/change-monaco-ui/SKILL.md`.
- Проверка целостности решения: `.cursor/skills/verify-data-console/SKILL.md`.
- Постоянные правила: `.cursor/rules/project-core.mdc`; файловые правила подключаются по glob.
